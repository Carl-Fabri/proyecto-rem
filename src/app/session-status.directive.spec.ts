import { SessionStatusDirective } from './session-status.directive';

describe('SessionStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new SessionStatusDirective();
    expect(directive).toBeTruthy();
  });
});
